# MedFlow 로드맵

> 기준일: 2026-08-20  
> 현재 코드에서 확인된 완료 범위와 실제 제약을 기준으로 한다. 기술 도입 자체를 목표로 삼지 않는다.

## Completed

### 인증과 역할

- `PATIENT`, `DOCTOR` 역할별 회원가입
- BCrypt 비밀번호, JWT Access/Refresh Token, DB Refresh Token 저장과 회전
- Spring Security Stateless 구성과 역할별 `@PreAuthorize`
- 프론트 인증 복원, 401 재발급, 역할별 라우트 가드
- 환경변수 기반 초기 `ADMIN` 생성

### 병원과 의사

- 활성 병원 검색·페이징과 상세 조회
- 활성 병원별 승인 의사 및 의사 상세 공개
- 의사 가입 승인 대기, 관리자 승인·반려
- 의사 프로필 조회·수정과 승인 후 인증 정보 변경 제한
- 승인 의사의 시간 구간 기반 진료 슬롯 일괄 생성·조회

### 예약

- 환자의 예약 가능 슬롯 선택과 즉시 승인 예약
- 환자 예약 목록의 상태·날짜·병원·의사·기간 필터와 페이징
- 본인 예약 취소와 슬롯 재개방
- 의사의 담당 예약 검색, 환자 기본 정보 조회
- 담당 의사가 `APPROVED` 예약을 직접 완료하는 API와 의사 예약 화면의 완료 버튼
- 관리자의 전체 예약 동적 조건 조회

### 문진과 AI

- 예약당 하나의 문진 작성·조회·수정
- 취소/완료 예약 및 타인 예약 접근 차단, 시작 후 수정 차단
- 문진 저장 커밋 후 별도 트랜잭션 분석
- Gemini JSON schema 기반 요약·핵심 내용·주의 신호·확인 항목·우선순위 생성
- 외부 분석 실패 시 문진 원본 유지와 `FAILED` 상태 저장
- 테스트/로컬용 Fake analyzer
- 환자와 담당 의사의 분석 결과 조회

### 관리와 배포

- 관리자 병원 등록·수정·soft delete
- 관리자 사용자·의사·예약 조회 화면과 대시보드
- Docker multi-stage 이미지, Nginx SPA/API proxy, MySQL Compose 구성
- GitHub Actions 백엔드 test/build, 프론트 lint/build, EC2 SSH 자동 배포
- 백엔드 Service, MVC 보안, JPA/QueryDSL, Entity, AI 분석 테스트

## Improvement

아래는 현재 코드에서 구체적인 문제 또는 경계가 확인된 항목이다.

### P0 — 데이터 정합성

1. **동시 예약의 DB 보장 추가**
   - 현재 예약 생성은 `DoctorSchedule.status`를 읽고 바꾸지만 슬롯 조회에 Lock이 없다.
   - `reservations.doctor_schedule_id`에도 UNIQUE가 없어 경합 시 DB가 중복 예약을 최종 차단하지 못한다.
   - 슬롯 Pessimistic Lock 또는 조건부 상태 변경과 DB UNIQUE 중 프로젝트에 맞는 방식을 결정하고 동시성 테스트를 추가한다.

2. **운영 DB migration 절차 도입**
   - `prod`는 `ddl-auto=validate`인데 저장소에 버전 관리 DDL, Flyway, Liquibase가 없다.
   - 도구 선택 이전에 현재 운영 스키마 기준선과 배포 순서, rollback 원칙을 먼저 정의한다.

3. **Refresh Token의 사용자당 한 행 보장**
   - Service는 `findByUser`로 한 행을 가정하지만 DB UNIQUE는 token에만 있다.
   - `user_id` UNIQUE 여부와 다중 기기 로그인을 허용할지 제품 정책을 결정한 뒤 Repository/제약을 일치시킨다.

### P1 — 사용자 흐름 완성도

1. **관리자 예약 검색을 서버 기준으로 통일**
   - 현재 화면의 병원·의사·환자 이름 검색은 서버에서 최대 100건을 받은 뒤 로컬 필터링한다.
   - 전체 결과의 정확한 검색과 페이징이 필요하면 백엔드에 이름/keyword 조건을 추가하고 화면 페이지 상태를 연결한다.

2. **문진 분석 재시도 정책**
   - `FAILED` 분석을 사용자가 재시도할 수 없고, 이벤트는 별도 비동기 executor나 durable queue 없이 같은 프로세스에서 실행된다.
   - 호출 지연 허용 범위, 재시도 횟수, 중복 실행 방지, 운영 실패 확인 방법을 먼저 정한 뒤 단순 executor 또는 외부 queue 필요성을 판단한다.

3. **의사 화면의 정보 범위 결정**
   - 현재 의사는 분석 결과만 보고 문진 원문 전체를 받지 않는다.
   - 의료진 업무상 원문 확인이 필요하다면 담당 관계 검증을 유지한 전용 응답을 설계한다.

4. **슬롯 관리 기능 보완**
   - 슬롯 생성·조회만 있고 수정, 삭제, 휴진, 예약된 슬롯 처리 정책이 없다.
   - 먼저 예약 없는 슬롯의 폐기와 예약된 슬롯 변경 시 환자 대응 규칙을 정한다.

### P1 — 계약과 보안

1. **서버 입력 검증 일관성**
   - 환자 프로필 PUT에 `@Valid`가 없다.
   - 문진 체온은 프론트만 30~45 범위를 제한하고 백엔드 DTO는 범위를 검증하지 않는다.
   - DoctorSchedule 필드의 DB NOT NULL도 JPA 매핑에 명시되지 않았다.

2. **API 경로·상태 계약 정리**
   - `/api/v1/reservations/`, `/api/v1/admin/hospitals/`처럼 trailing slash가 혼재한다.
   - 프론트 `ReservationStatus`에는 백엔드에 없는 `PENDING`, `REJECTED`가 남아 있다.
   - 분석 타입은 대기·실패 상태에서도 결과 필드를 non-null로 표현한다.
   - Swagger 제목도 현재 프로젝트 명이 아니라 테스트용 문구다.

3. **토큰 저장 위협 모델 검토**
   - 프론트는 Access/Refresh Token을 모두 localStorage에 저장한다.
   - 실제 운영의 XSS 방어, CSP, Refresh Token을 HttpOnly cookie로 옮길 필요, 로그아웃/탈퇴 시 폐기 범위를 검토한다.

4. **Soft delete 정책 일치**
   - 전역 필터가 없어 조회마다 상태 조건을 정확히 넣어야 한다.
   - 의사 사용자가 탈퇴해도 Doctor의 `deleted_at`은 갱신되지 않고, 탈퇴 UI는 환자에게만 노출된다.
   - 역할별 프로필 보존 목적과 개인정보 삭제 범위를 문서화한다.

### P2 — 운영과 품질

2. **환경별 호스트 설정 외부화**
   - CORS와 `.env.example`에 현재 IP가 고정되어 있다.
   - 개발/운영 허용 origin과 API base URL을 배포 환경변수로 일관되게 관리한다.

3. **프론트엔드 자동 테스트 추가**
   - 현재 lint/build만 실행한다.
   - 우선순위는 토큰 재발급 단일화, 역할 가드, 예약/문진 Mutation 후 cache 갱신 같은 회귀 위험이 큰 흐름이다.

4. **실제 E2E 경로 검증**
   - 회원가입 → 의사 승인 → 슬롯 생성 → 환자 예약 → 문진 → 분석 → 의사 확인 흐름을 자동으로 검증하는 테스트가 없다.
   - 외부 Gemini는 Fake로 대체하고 핵심 브라우저 흐름부터 최소 범위로 구성한다.

5. **관리자 통계의 집계 정확성**
   - 대시보드 일부 상태 차트는 최대 100건 응답을 기반으로 계산하지만 전체 값처럼 보일 수 있다.
   - 데이터 규모가 커지면 전용 집계 API 또는 정확한 서버 집계를 설계한다.

6. **시간 기준 통일**
   - 문진 수정 가능 시각과 예약 기간 검색, Refresh Token 만료 일부는 `LocalDateTime.now()`/`LocalDate.now()` 또는 시스템 기본 timezone을 직접 사용한다.
   - 서버·DB·브라우저 timezone이 다를 때 예약 경계가 어긋나지 않도록 업무 timezone과 테스트 가능한 `Clock` 사용 범위를 통일한다.

## Future

아래 항목은 현재 구현 문제가 아니라 제품 요구가 확정될 때 검토할 후보다. 구현 기술은 요구 규모와 실패 허용 수준을 확인한 뒤 선택한다.

- 환자 예약 변경/재예약과 병원·의사 일정 변경 통지
- 예약·문진 작성 알림 및 미작성 문진 안내
- 병원별 관리자 또는 다중 관리자 권한 모델
- 의사 휴진, 반복 일정, 예외 일정 관리
- 사용자 잠금·복구와 관리자 감사 이력
- 문진 분석 재실행, 모델/프롬프트 버전, 결과 생성 이력 관리
- 운영 관측성: 구조화 로그, 상태/지연 메트릭, AI 실패율과 예약 경합 모니터링

Redis, Kafka, Kubernetes 같은 기술은 위 요구를 해결하는 최소 복잡도인지 검증된 경우에만 후보로 삼는다.
