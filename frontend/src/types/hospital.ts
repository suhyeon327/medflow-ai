export interface HospitalListItem {
  id: number;
  name: string;
  region: string;
  address: string;
}

export interface HospitalDetail {
  id: number;
  name: string;
  address: string;
  region: string;
  tel: string;
}